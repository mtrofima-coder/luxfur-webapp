# Lux Fur — Telegram Mini App

Премиальный минималистичный сайт для бренда Lux Fur, полностью совместимый с Telegram Mini App.

## 🚀 Технологии

- **Next.js 16** — React фреймворк
- **TypeScript** — типизация
- **Tailwind CSS** — стилизация
- **Telegram WebApp API** — интеграция с Telegram
- **Cormorant Garamond** — премиальная типографика для заголовков
- **Inter** — современный шрифт для текста

## 📋 Настройка Telegram Bot

### Шаг 1: Создание бота через @BotFather

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Укажите имя бота (например: "Lux Fur Shop")
   - Укажите username бота (например: `luxfur_shop_bot`)
4. Сохраните **Bot Token**, который выдаст BotFather (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 2: Настройка Mini App

1. Отправьте BotFather команду `/newapp`
2. Выберите вашего бота из списка
3. Укажите название приложения (например: "Lux Fur Catalog")
4. Укажите короткое описание
5. Загрузите фото (опционально, 640x360px)
6. Укажите **Web App URL** — адрес вашего приложения:
   ```
   https://your-domain.com
   ```
   Или для разработки:
   ```
   https://your-ngrok-url.ngrok.io
   ```

### Шаг 3: Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
```

**Важно:** Никогда не коммитьте `.env.local` в Git!

### Шаг 4: Получение Bot Token для проверки подписи

Bot Token нужен для проверки подлинности данных от Telegram через эндпоинт `/api/telegram/verify`.

## 🛠️ Установка и запуск

### Установка зависимостей

```bash
npm install
# или
yarn install
# или
pnpm install
```

### Разработка

```bash
npm run dev
# или
yarn dev
# или
pnpm dev
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

### Для тестирования в Telegram Mini App

1. Используйте [ngrok](https://ngrok.com/) или аналогичный сервис для создания туннеля:
   ```bash
   ngrok http 3000
   ```

2. Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)

3. В BotFather укажите этот URL как Web App URL

4. Откройте бота в Telegram и нажмите на кнопку "Open App" или отправьте команду `/start`

## 📱 Функции Telegram Mini App

### Автоматическая инициализация

- ✅ `WebApp.ready()` — автоматически вызывается при загрузке
- ✅ `WebApp.expand()` — автоматически расширяет приложение на всю высоту
- ✅ Определение темы (light/dark) из настроек Telegram
- ✅ Адаптация под высоту viewport

### Безопасный режим

Приложение работает даже если открыто не в Telegram (fallback режим):
- Определяет наличие Telegram WebApp API
- Корректно обрабатывает отсутствие API
- Сохраняет функциональность в обычном браузере

### Проверка подписи initData

Эндпоинт `/api/telegram/verify` позволяет проверить подлинность данных от Telegram:

```typescript
const response = await fetch('/api/telegram/verify', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ initData: webApp.initData })
});

const { verified } = await response.json();
```

## 🎨 Дизайн

- **Минималистичный** чёрно-белый дизайн
- **Премиальная типографика** — Cormorant Garamond для заголовков
- **Адаптивная вёрстка** — идеально для мобильных устройств
- **Плавные анимации** — fade-in эффекты
- **Поддержка 3 языков** — RU, EN, AR

## 📂 Структура проекта

```
luxfur-webapp/
├── app/
│   ├── api/
│   │   └── telegram/
│   │       └── verify/          # Эндпоинт проверки подписи
│   ├── catalog/                  # Страница каталога
│   ├── layout.tsx                # Корневой layout с Telegram интеграцией
│   ├── page.tsx                  # Главная страница
│   └── globals.css               # Глобальные стили
├── components/
│   └── TelegramProvider.tsx     # Провайдер для инициализации Telegram
├── hooks/
│   └── useTelegram.ts           # React хук для работы с Telegram
├── lib/
│   └── telegram.ts              # Утилиты для Telegram WebApp API
├── types/
│   └── telegram.d.ts            # TypeScript типы для Telegram API
└── public/
    └── products/                 # Изображения товаров
```

## 🔧 Использование Telegram API

### React хук

```typescript
import { useTelegram } from '@/hooks/useTelegram';

function MyComponent() {
  const { webApp, isReady, theme, user, isTelegram } = useTelegram();

  if (!isReady) return <div>Loading...</div>;

  return (
    <div>
      {isTelegram ? (
        <p>Привет, {user?.first_name}!</p>
      ) : (
        <p>Откройте в Telegram для полного функционала</p>
      )}
    </div>
  );
}
```

### Прямое использование утилит

```typescript
import { getTelegramWebApp, getTelegramUser } from '@/lib/telegram';

const webApp = getTelegramWebApp();
if (webApp) {
  webApp.MainButton.setText('Заказать');
  webApp.MainButton.show();
  webApp.MainButton.onClick(() => {
    // Обработка клика
  });
}
```

## 🚢 Деплой

### Vercel

1. Подключите репозиторий к Vercel
2. Добавьте переменную окружения `TELEGRAM_BOT_TOKEN` в настройках проекта
3. Обновите Web App URL в BotFather на ваш домен Vercel

### Другие платформы

Убедитесь, что:
- Переменная `TELEGRAM_BOT_TOKEN` установлена
- Приложение доступно по HTTPS
- Web App URL в BotFather указывает на ваш домен

## 📝 Лицензия

Private project — Lux Fur

## 🔗 Полезные ссылки

- [Telegram Bot API Documentation](https://core.telegram.org/bots/api)
- [Telegram WebApp API Documentation](https://core.telegram.org/bots/webapps)
- [Next.js Documentation](https://nextjs.org/docs)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)
